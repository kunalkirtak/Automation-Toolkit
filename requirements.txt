# Core dependencies
openpyxl>=3.1.2        # Excel (.xlsx) read/write/formatting
pypdf>=4.0.0            # PDF merge/split/encrypt/decrypt/metadata
pandas>=2.0.0           # Data filtering, summaries, CSV/report export
tqdm>=4.66.0            # Progress bars for batch operations

# Dev / testing dependencies
pytest>=8.0.0
pytest-cov>=5.0.0
